const validName = 'Martelo de Thor';
const validPrice = '30 peças de ouro';
const validOrderId = 4;

const validProduct = { name: validName, price: validPrice, orderId: validOrderId };

const validResponse = { id: 6, name: validName, price: validPrice, orderId: validOrderId };

const validList = {
  id: 1,
  name: 'Excalibur',
  price: '10 peças de ouro',
  orderId: 1
}

export default {
  validProduct,
  validResponse,
  validList,
}